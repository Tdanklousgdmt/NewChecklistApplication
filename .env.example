# Frontend: optional absolute API URL (default = Supabase Edge make-server-d5ac9b81).
# VITE_API_BASE_URL=https://YOUR_REF.supabase.co/functions/v1/make-server-d5ac9b81

# Edge function / Vercel Node API:
# - Real JWT: SUPABASE_JWT_SECRET or APP_JWT_SECRET (Project Settings → API → JWT Secret)
# - Role-picker demo: sends anon key + X-App-Role; function needs SUPABASE_ANON_KEY secret
#   matching the anon key the app uses (same as in utils/supabase/info).
